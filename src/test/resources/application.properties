# Tests use an isolated in-memory database and never touch the local data folder.
spring.datasource.url=jdbc:h2:mem:menu-manager-test;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=false
spring.h2.console.enabled=false
